<jpml id="HEADLINE-IMAGE_module">
	<style src="GLOBAL_MODULES-stylesheet.ejs" />
	<style src="GLOBAL-stylesheet.ejs" />
	<style src="PHONE-MODULES-stylesheet.ejs" />
	<style>{
		$join: [
			{ $switch: [
				{ if: =options.no_border, then: {
					#wrapper: {
						padding: [10, 0, {if: =options.bottom, then:20, else:10}, 0],
						border-width: [0],
						border-color:white
					}
			
				}},
				{ if: true, then: {
					#wrapper: {
						border-width: [1, 0, 0, 0],
						border-color: #b1aca1,
						padding: [10, 0, {if: =options.bottom, then:20, else:10}, 0]
					}
					}}
			]},
			{#padding: [20, 8] },
			{#body: { 
	            column-count: {$switch: [
					{if: =widescreen, then: { if: =options.two_columns, then:2, else:1 }},
					{if: =scale.large, then: {$divide: [=view.width, {$multiply:[250, =scale.dpi]}]}}
					
				]},
	            column-gutter: 20
	        }},
			{#body_container: { padding: [0, 8]}},
			{#headline: { padding: [0, 8]}},
			{#tablet_image: {
				width:{$subtract:[=view.width, 18]}, 
				height:{$multiply: [{$subtract:[=view.width, 18]}, {if: =options.F_image, then:0.39, else:0.66}] },
				display: { if: =options.show_image, then:inherit, else:none },
				padding: [0, 9, 8, 9],
				options: {
					match: {
						if: =options.F_image, then: { width:571, height:226, max-height:226 }
						}
				},
				pixels-per-point-x:1,
				pixels-per-point-y:1,
                image-scale: {$max:[1,{$divide:[{$subtract:[=view.width, 18]}, 553]}]}
			}},
			{.invisible: { display:none }},
			{#small_headline: { display: { if: =options.small_headline, then:inherit }}},
			{#medium_headline: { display: { if: =options.medium_headline, then:inherit }}},
			{#large_headline: { display: { if: =options.large_headline, then:inherit }}},
			{.phone:{display:none}}
			]
		}</style>
		<style>{
			if: =scale.phone, then: {
				#body_container: {padding:[0]},
				#headline: {padding:[0]},
				.tablet: {display:none},
				.phone:{display:inherit},
				#wrapper: {padding:[4, 8], border-color:#d6d6d6},
				#image_wrapper: { 
					anchor-x:right,
					position:relative,
					width: 76,
					padding:[0, 0, 5, 15],
					pixels-per-point-x:1,
					pixels-per-point-y:1,
					image-scale:1,
					display:inherit
				},
				#phone_image: { 
					height: 76,
					display:inherit,
					padding: [0],
					image-scale: 1
				
				}
				
			}, else:{}
			
		}</style>
	<box id="wrapper">
		<box id="image_wrapper" class="phone">
			<image embed="summary-image" class="phone" id="phone_image" target-width="76" target-height="76" />
		</box>
		<panel id="headline">
			<p id="small_headline" class="invisible"><span embed="headline" /><mark class="key" /></p>
			<p id="medium_headline" class="invisible"><span embed="headline" /><mark class="key" /></p>
			<p id="large_headline" class="invisible"><span embed="headline" /><mark class="key" /></p>
		</panel>
		<image class="tablet" id="tablet_image" embed="summary-image"/>
		
		<panel id="body_container">
	        <panel id="body">
	            <p class="byline" embed="byline"/>
	            <panel class="body" embed="body"/>
	        </panel>
	    </panel>
	</box>
</jpml>
