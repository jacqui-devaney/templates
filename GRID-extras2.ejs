{
	jpml: {
		padding: [{
			$switch: [
				{ if-all: [
					=scale.tablet,
					=intent.template
				], then: 68 },
				{ if-all: [
					=scale.phone,
					=intent.template,
					{ if: =page.section, equals: "TOP_NEWS" }
				], then: 0 }
			]
		}, 0, 0, 0]
	},
	.vertical-grid-module: {
		layout-grid-line-offset: 5,
		layout-grid-line: { 
			if: =intent.article, then: { $multiply: [20, =view.zoomlevel] }, else: 20 
		}
	},
	.small-vertical-grid-module: {
		layout-grid-line-offset: -31,
		layout-grid-line: { 
			if: =intent.article, then: { $multiply: [20, =view.zoomlevel] }, else: 20 
		}
	},
	.visual-content-padding: {
		padding: [10, 0, 10, 0]
	},
	.visual-content-top: {
		top: 10
	}
}
