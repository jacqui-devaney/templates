{$join: [
	{jpml: {
		border-width:[1, 0, 0, 0], 
		border-color:black
	}},
{if: =portrait, then: {
		#whatsnews: { 
			height:960,
			top:0, 
			options: { expanded-frame: [
				{$multiply:[=scale.pagex, 3]}, 
				{$multiply:[=scale.pagey, 5]}, 
				{$multiply:[=scale.pagex, 550]}, 
				{$multiply:[=scale.pagey, 960]}
			] }
		}
	}, else: {
		#whatsnews:		{ 
			height:706,
			top:0, 
			options: { expanded-frame: [
				{$multiply:[=scale.pagex, 3]}, 
				{$multiply:[=scale.pagey, 5]}, 
				{$multiply:[=scale.pagex, 550]}, 
				{$multiply:[=scale.pagey, 706]}
			] }
		}
}}

]}