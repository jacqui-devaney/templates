{$join: [
	{jpml: {
		border-width:[1, 0, 0, 0], 
		border-color:black
	}},
{if: =portrait, then: {
		#whatsnews: { 
			height:{if: =issue.type, equals:"ITP", then:960, else:915},
			top:{if: =issue.type, equals:"ITP", then:0, else:45}, 
			options: { expanded-frame: [
				{$multiply:[=scale.pagex, 3]}, 
				{$multiply:[=scale.pagey, 5]}, 
				{$multiply:[=scale.pagex, 550]}, 
				{$multiply:[=scale.pagey, 960]}
			] }
		}
	}, else: {
		#whatsnews:		{ 
			height:{if: =issue.type, equals:"ITP", then:706, else:661},
			top:{if: =issue.type, equals:"ITP", then:0, else:45}, 
			options: { expanded-frame: [
				{$multiply:[=scale.pagex, 3]}, 
				{$multiply:[=scale.pagey, 5]}, 
				{$multiply:[=scale.pagex, 550]}, 
				{$multiply:[=scale.pagey, 706]}
			] }
		}
}}

]}