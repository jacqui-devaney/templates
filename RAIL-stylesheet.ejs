{ if: =scale.rail, then: {
	jpml: {
		image-scale: 1,
		pixels-per-point-x: 1,
		pixels-per-point-y: 1,
		padding:[0]
	},

	.unit_plain: {
		options: {
			module: url(NAVIGATION_module.xml),
			medium_headline: true,
			show_body: true,
			show_byline: { if: =page.section, equals: OPINION }
		},
		border-width: [0, 0, 1, 0],
		border-color: #DDDDDD
	},
	.unit_rando: {
		options: {
			module: url(NAVIGATION_module.xml),
			medium_headline: true,
			show_body: true,
			show_subhed: true
		},
		border-width: [0, 0, 1, 0],
		border-color: #DDDDDD
	},
	.unit_deco: {
		options: {
			module: url(NAVIGATION_module.xml),
			medium_headline: true,
			show_body: true
		},
		border-width: [0, 0, 1, 0],
		border-color: #DDDDDD
	},
	.unit_full: {
		options: {
			module: url(NAVIGATION_module.xml),
			medium_headline: true,
			show_body: true
		},
		border-width: [0, 0, 1, 0],
        border-color: #DDDDDD
	},
	.unit_deloitte: {
		options: {
			module: url(NAVIGATION-deloitte_module.xml),
			medium_headline: true,
			show_body: true
		},
		border-width: [0, 0, 1, 0],
        border-color: #DDDDDD
	},
    .unit_adobe: {
        options: {
            module: url(NAVIGATION-adobe_module.xml),
            medium_headline: true,
            show_body: true
        },
        border-width: [0, 0, 1, 0],
        border-color: #DDDDDD
    },
    .unit_ups: {
        options: {
            module: url(NAVIGATION-ups_module.xml),
            medium_headline: true,
            show_body: true
        },
        border-width: [0, 0, 1, 0],
        border-color: #DDDDDD
    },
	#b1: {
		height: {
			if: {$count:[=articles.b1.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b2: {
		height: {
			if: {$count:[=articles.b2.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b3: {
		height: {
			if: {$count:[=articles.b3.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b4: {
		height: {
			if: {$count:[=articles.b4.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b5: {
		height: {
			if: {$count:[=articles.b5.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b6: {
		height: {
			if: {$count:[=articles.b6.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b7: {
		height: {
			if: {$count:[=articles.b7.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b8: {
		height: {
			if: {$count:[=articles.b8.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b9: {
		height: {
			if: {$count:[=articles.b9.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b10: {
		height: {
			if: {$count:[=articles.b10.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b11: {
		height: {
			if: {$count:[=articles.b11.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b12: {
		height: {
			if: {$count:[=articles.b12.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b13: {
		height: {
			if: {$count:[=articles.b13.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b14: {
		height: {
			if: {$count:[=articles.b14.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b15: {
		height: {
			if: {$count:[=articles.b15.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b16: {
		height: {
			if: {$count:[=articles.b16.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b17: {
		height: {
			if: {$count:[=articles.b17.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b18: {
		height: {
			if: {$count:[=articles.b18.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b19: {
		height: {
			if: {$count:[=articles.b19.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b20: {
		height: {
			if: {$count:[=articles.b20.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b21: {
		height: {
			if: {$count:[=articles.b21.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b22: {
		height: {
			if: {$count:[=articles.b22.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b23: {
		height: {
			if: {$count:[=articles.b23.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b24: {
		height: {
			if: {$count:[=articles.b24.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b25: {
		height: {
			if: {$count:[=articles.b25.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b26: {
		height: {
			if: {$count:[=articles.b26.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b27: {
		height: {
			if: {$count:[=articles.b27.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b28: {
		height: {
			if: {$count:[=articles.b28.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b29: {
		height: {
			if: {$count:[=articles.b29.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b30: {
		height: {
			if: {$count:[=articles.b30.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b31: {
		height: {
			if: {$count:[=articles.b31.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#b32: {
		height: {
			if: {$count:[=articles.b32.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#deco: {
		height: {
			if: {$count:[=articles.deco.images.rail]},
			more-than: 0,
			then: =size.rail_height_with_image,
			else: =size.rail_height_without_image
		}
	},
	#d1: {
		height: 100
	},
	#d2: {
		height: 100
	},
	#d3: {
		height: 100
	},
	#d4: {
		height: 100
	},
	#d5: {
		height: 100
	},
	#d6: {
		height: 100
	}
}}
