{
	#b1 : {
		options: {bind: {if:=scale.phone,then:none}} 
	},
	#b2 : {
		options: {hide_strap: {if:=articles.b2.subsection, equals:=articles.b1.subsection}, bind: {if:=scale.phone,then:none}, reuse:{if:=scale.phone,then:continuation}} 
	},
    #b3 : {
		options: {hide_strap: {if:=articles.b3.subsection, equals:=articles.b2.subsection}, bind: {if:=scale.phone,then:none}, reuse:{if:=scale.phone,then:continuation}} 
	},
    #b4 : {
		options: {hide_strap: {if:=articles.b4.subsection, equals:=articles.b3.subsection}, bind: {if:=scale.phone,then:none}, reuse:{if:=scale.phone,then:continuation}} 
	},
    #b5 : {
		options: {hide_strap: {if:=articles.b5.subsection, equals:=articles.b4.subsection}, bind: {if:=scale.phone,then:none}, reuse:{if:=scale.phone,then:continuation}} 
	},
	#b6 : {
		options: {hide_strap: {if:=articles.b6.subsection, equals:=articles.b5.subsection}, bind: {if:=scale.phone,then:none}, reuse:{if:=scale.phone,then:continuation}} 
	},
	#b7 : {
		options: {hide_strap: {if:=articles.b7.subsection, equals:=articles.b6.subsection}, bind: {if:=scale.phone,then:none}, reuse:{if:=scale.phone,then:continuation}} 
	},
    #b8 : {
		options: {hide_strap: {if:=articles.b8.subsection, equals:=articles.b7.subsection}, bind: {if:=scale.phone,then:none}, reuse:{if:=scale.phone,then:continuation}} 
	},
    #b9 : {
		options: {hide_strap: {if:=articles.b9.subsection, equals:=articles.b8.subsection}, bind: {if:=scale.phone,then:none}, reuse:{if:=scale.phone,then:continuation}} 
	},
    #b10: {
		options: {hide_strap: {if:=articles.b10.subsection, equals:=articles.b9.subsection}, bind: {if:=scale.phone,then:none}, reuse:{if:=scale.phone,then:continuation}} 
	},
    #b11: {
		options: {hide_strap: {if:=articles.b11.subsection, equals:=articles.b10.subsection}, bind: {if:=scale.phone,then:none}, reuse:{if:=scale.phone,then:continuation}} 
	},
    #b12: {
		options: {hide_strap: {if:=articles.b12.subsection, equals:=articles.b11.subsection}, bind: {if:=scale.phone,then:none}, reuse:{if:=scale.phone,then:continuation}} 
		}

}
